<%= form_with(model: copyright, local: true) do |form| %>
  <% if copyright.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(copyright.errors.count, "error") %> prohibited this copyright from being saved:</h2>

      <ul>
      <% copyright.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :copyright_name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :copyright_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
